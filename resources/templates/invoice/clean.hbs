<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{t "invoices.invoice"}} {{invoice.number}}</title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f8fafc;
            color: #1a202c;
            line-height: 1.6;
        }
        
        .invoice-container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            overflow: hidden;
        }
        
        .header-section {
            background: white;
            padding: 40px 40px 20px 40px;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }
        
        .header-left {
            flex: 1;
        }
        
        .invoice-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #1a202c;
            margin: 0 0 10px 0;
        }
        
        .invoice-number {
            font-size: 1.1rem;
            color: #3b82f6;
            font-weight: 500;
            margin-bottom: 20px;
        }
        
        .invoice-meta {
            display: flex;
            flex-direction: column;
            gap: 8px;
            background: #f8fafc;
            padding: 20px;
            border-radius: 6px;
            border: 1px solid #e2e8f0;
            min-width: 200px;
        }
        
        .meta-row {
            display: flex;
            justify-content: space-between;
            font-size: 0.9rem;
        }
        
        .meta-label {
            color: #64748b;
            font-weight: 500;
        }
        
        .meta-value {
            color: #1a202c;
            font-weight: 500;
        }
        
        .main-content {
            padding: 40px;
        }
        
        .parties-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }
        
        .party-card {
            background: #f8fafc;
            border-radius: 6px;
            padding: 24px;
            border: 1px solid #e2e8f0;
        }
        
        .party-title {
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            color: #64748b;
            margin-bottom: 16px;
            letter-spacing: 0.5px;
        }
        
        .company-name {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1a202c;
            margin-bottom: 8px;
        }
        
        .party-info {
            color: #4a5568;
            font-size: 0.9rem;
            line-height: 1.6;
        }
        
        .party-info div {
            margin-bottom: 4px;
        }
        
        .party-info strong {
            color: #1a202c;
        }
        
        .items-section {
            margin-bottom: 40px;
        }
        
        .items-table {
            width: 100%;
            border-collapse: collapse;
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            overflow: hidden;
        }
        
        .items-table th {
            background: #f8fafc;
            color: #374151;
            padding: 16px 12px;
            text-align: left;
            font-weight: 600;
            font-size: 0.85rem;
            border-bottom: 2px solid #e2e8f0;
        }
        
        .items-table th:last-child,
        .items-table td:last-child {
            text-align: right;
        }
        
        .items-table td {
            padding: 16px 12px;
            border-bottom: 1px solid #f1f5f9;
            vertical-align: top;
        }
        
        .items-table tbody tr:hover {
            background: #f8fafc;
        }
        
        .items-table tbody tr:last-child td {
            border-bottom: none;
        }
        
        .item-description {
            font-weight: 500;
            color: #1a202c;
            margin-bottom: 4px;
        }
        
        .item-vat {
            font-size: 0.8rem;
            color: #64748b;
        }
        
        .item-total {
            font-weight: 600;
            color: #1a202c;
        }
        
        .summary-section {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 40px;
        }
        
        .summary-card {
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 6px;
            padding: 24px;
            min-width: 300px;
        }
        
        .summary-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            padding-bottom: 12px;
            border-bottom: 1px solid #f1f5f9;
        }
        
        .summary-row:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
            font-size: 1.1rem;
            font-weight: 700;
            margin-top: 8px;
            padding-top: 12px;
            border-top: 2px solid #e2e8f0;
        }
        
        .summary-label {
            color: #64748b;
            font-weight: 500;
        }
        
        .summary-value {
            color: #1a202c;
            font-weight: 500;
        }
        
        .payment-section {
            background: #f8fafc;
            border-radius: 6px;
            padding: 24px;
            margin-bottom: 40px;
            border: 1px solid #e2e8f0;
        }
        
        .payment-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1a202c;
            margin-bottom: 16px;
        }
        
        .payment-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }
        
        .payment-info {
            font-size: 0.9rem;
            color: #4a5568;
            line-height: 1.6;
        }
        
        .payment-info p {
            margin-bottom: 8px;
        }
        
        .payment-info strong {
            color: #1a202c;
        }
        
        .bank-section {
            background: white;
            padding: 20px;
            border-radius: 6px;
            border: 1px solid #e2e8f0;
            margin-top: 20px;
        }
        
        .bank-title {
            font-weight: 600;
            color: #1a202c;
            margin-bottom: 12px;
            font-size: 0.9rem;
        }
        
        .bank-details {
            font-size: 0.85rem;
            color: #4a5568;
            line-height: 1.6;
        }
        
        .bank-details p {
            margin-bottom: 6px;
        }
        
        .bank-details strong {
            color: #1a202c;
        }
        
        .footer-section {
            background: #f8fafc;
            padding: 20px 40px;
            border-top: 1px solid #e2e8f0;
            text-align: center;
        }
        
        .footer-section img {
            max-height: 40px;
            opacity: 0.6;
        }
        
        .footer-text {
            font-size: 0.8rem;
            color: #64748b;
            margin-top: 10px;
        }
        
        .text-right {
            text-align: right;
        }
        
        .text-center {
            text-align: center;
        }
        
        .font-semibold {
            font-weight: 600;
        }
        
        .font-bold {
            font-weight: 700;
        }
        
        .mb-2 {
            margin-bottom: 8px;
        }
        
        .mb-4 {
            margin-bottom: 16px;
        }
        
        .opacity-75 {
            opacity: 0.75;
        }
        
        @media print {
            body {
                background: white;
                padding: 0;
            }
            
            .invoice-container {
                box-shadow: none;
                border-radius: 0;
            }
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <!-- Header Section -->
        <div class="header-section">
            <div class="header-content">
                <div class="header-left">
                    <h1 class="invoice-title">{{t "invoices.invoice"}}</h1>
                    <div class="invoice-number">{{invoice.number}}</div>
                </div>
                <div class="invoice-meta">
                    {{#if invoice.issueDate}}
                        <div class="meta-row">
                            <span class="meta-label">{{t "invoices.issue_date"}}</span>
                            <span class="meta-value">{{invoice.issueDate}}</span>
                        </div>
                    {{/if}}
                    {{#if invoice.dueDate}}
                        <div class="meta-row">
                            <span class="meta-label">{{t "invoices.due_date"}}</span>
                            <span class="meta-value">{{invoice.dueDate}}</span>
                        </div>
                    {{/if}}
                </div>
            </div>
        </div>

        <div class="main-content">
            <!-- Business Information -->
            <div class="parties-section">
                <div class="party-card">
                    <div class="party-title">{{t "invoices.from"}}</div>
                    <div class="party-info">
                        {{#if invoice.seller.name}}
                            <div class="company-name">{{invoice.seller.name}}</div>
                        {{/if}}
                        {{#if invoice.seller.address}}
                            <div>{{invoice.seller.address}}</div>
                        {{/if}}
                        {{#if invoice.seller.country}}
                            <div>{{invoice.seller.country}}</div>
                        {{/if}}
                        {{#if invoice.seller.taxId}}
                            <div><strong>{{t "invoices.tax_id"}}:</strong> {{invoice.seller.taxId}}</div>
                        {{/if}}
                        {{#if invoice.seller.email}}
                            <div><strong>{{t "invoices.email"}}:</strong> {{invoice.seller.email}}</div>
                        {{/if}}
                    </div>
                </div>

                <div class="party-card">
                    <div class="party-title">{{t "invoices.to"}}</div>
                    <div class="party-info">
                        {{#if invoice.buyer.name}}
                            <div class="company-name">{{invoice.buyer.name}}</div>
                        {{/if}}
                        {{#if invoice.buyer.address}}
                            <div>{{invoice.buyer.address}}</div>
                        {{/if}}
                        {{#if invoice.buyer.country}}
                            <div>{{invoice.buyer.country}}</div>
                        {{/if}}
                        {{#if invoice.buyer.taxId}}
                            <div><strong>{{t "invoices.tax_id"}}:</strong> {{invoice.buyer.taxId}}</div>
                        {{/if}}
                        {{#if invoice.buyer.email}}
                            <div><strong>{{t "invoices.email"}}:</strong> {{invoice.buyer.email}}</div>
                        {{/if}}
                    </div>
                </div>
            </div>

            <!-- Description -->
            {{#if invoice.description}}
                <div class="payment-section mb-4">
                    <h3 class="payment-title">{{t "invoices.description"}}</h3>
                    <div class="payment-info">{{invoice.description}}</div>
                </div>
            {{/if}}

            <!-- Items Table -->
            <div class="items-section">
                <table class="items-table">
                    <thead>
                        <tr>
                            <th>{{t "invoices.description"}}</th>
                            <th class="text-center">{{t "invoices.quantity"}}</th>
                            <th class="text-right">{{t "invoices.unit_price"}}</th>
                            <th class="text-right">{{t "invoices.total_net"}}</th>
                            <th class="text-right">{{t "invoices.vat"}}</th>
                            <th class="text-right">{{t "invoices.total_gross"}}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each invoice.lines}}
                            <tr>
                                <td>
                                    {{#if description}}
                                        <div class="item-description">{{description}}</div>
                                    {{/if}}
                                    <div class="item-vat">{{vatRateName}} ({{vatRateValue}}%)</div>
                                </td>
                                <td class="text-center">{{formattedQuantity}}</td>
                                <td class="text-right">{{formattedUnitPrice}}</td>
                                <td class="text-right">{{formattedTotalNet}}</td>
                                <td class="text-right">{{formattedTotalVat}}</td>
                                <td class="item-total">{{formattedTotalGross}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>

            <!-- VAT Summary -->
            {{#if invoice.vatSummary}}
                <div class="items-section">
                    <table class="items-table">
                        <thead>
                            <tr>
                                <th>{{t "invoices.vat_rate"}}</th>
                                <th class="text-right">{{t "invoices.net_amount"}}</th>
                                <th class="text-right">{{t "invoices.vat_amount"}}</th>
                                <th class="text-right">{{t "invoices.gross_amount"}}</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each invoice.vatSummary}}
                                <tr>
                                    <td>{{vatRateName}} ({{vatRateValue}}%)</td>
                                    <td class="text-right">{{formattedNet}}</td>
                                    <td class="text-right">{{formattedVat}}</td>
                                    <td class="text-right font-semibold">{{formattedGross}}</td>
                                </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            {{/if}}

            <!-- Total Summary -->
            <div class="summary-section">
                <div class="summary-card">
                    <div class="summary-row">
                        <span class="summary-label">{{t "invoices.subtotal"}}</span>
                        <span class="summary-value">{{invoice.formattedTotalNet}}</span>
                    </div>
                    <div class="summary-row">
                        <span class="summary-label">{{t "invoices.vat"}}</span>
                        <span class="summary-value">{{invoice.formattedTotalTax}}</span>
                    </div>
                    <div class="summary-row">
                        <span class="summary-label">{{t "invoices.total"}}</span>
                        <span class="summary-value">{{invoice.formattedTotalGross}}</span>
                    </div>
                </div>
            </div>

            <!-- Payment Information -->
            {{#if invoice.payment}}
                <div class="payment-section">
                    <h3 class="payment-title">{{t "invoices.payment_details"}}</h3>
                    <div class="payment-content">
                        <div class="payment-info">
                            <p><strong>{{t "invoices.payment_method"}}:</strong> {{invoice.payment.method}}</p>
                            <p><strong>{{t "invoices.payment_status"}}:</strong> {{invoice.payment.status}}</p>
                            {{#if invoice.payment.reference}}
                                <p><strong>{{t "invoices.reference"}}:</strong> {{invoice.payment.reference}}</p>
                            {{/if}}
                        </div>
                        <div class="payment-info">
                            {{#if invoice.payment.formattedPaidAmount}}
                                <p><strong>{{t "invoices.paid_amount"}}:</strong> {{invoice.payment.formattedPaidAmount}}</p>
                            {{/if}}
                            {{#if invoice.payment.paidDate}}
                                <p><strong>{{t "invoices.paid_date"}}:</strong> {{invoice.payment.paidDate}}</p>
                            {{/if}}
                        </div>
                    </div>

                    {{#if invoice.payment.bankAccount}}
                        <div class="bank-section">
                            <div class="bank-title">{{t "invoices.bank_account"}}</div>
                            <div class="bank-details">
                                {{#if invoice.payment.bankAccount.bankName}}
                                    <p><strong>{{t "invoices.bank_name"}}:</strong> {{invoice.payment.bankAccount.bankName}}</p>
                                {{/if}}
                                {{#if invoice.payment.bankAccount.iban}}
                                    <p><strong>{{t "invoices.iban"}}:</strong> {{invoice.payment.bankAccount.iban}}</p>
                                {{/if}}
                                {{#if invoice.payment.bankAccount.swift}}
                                    <p><strong>{{t "invoices.swift"}}:</strong> {{invoice.payment.bankAccount.swift}}</p>
                                {{/if}}
                            </div>
                        </div>
                    {{/if}}
                </div>
            {{/if}}
        </div>

        <!-- Footer -->
        <div class="footer-section">
            {{#if invoice.seller.logoUrl}}
                {{{logoUrl invoice.seller.logoUrl}}}
            {{/if}}
            <div class="footer-text">
                {{t "invoices.invoice"}} {{invoice.number}}
            </div>
        </div>
    </div>
</body>
</html>