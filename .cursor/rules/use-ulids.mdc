---
description: Identyfikatory modeli powinny być UUID — nie używamy autoinkrementacji.
globs: app/Domain/**/Models/*.php
alwaysApply: false
---

- All Eloquent models should use ULIDs as primary keys.
- Do not use auto-incrementing integers.
- Use BaseModel that handles that.
